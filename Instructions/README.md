# Operator instructions

These files are for people who will **install and run** `waveform` on Ubuntu/WSL. They are not the architecture spec (`docs/`).

1. [`install.md`](./install.md) — Ubuntu 24.04 / WSL2, FFmpeg, `uv`, clone, `waveform doctor`.
2. [`runbook.md`](./runbook.md) — daily CLI, outputs, SKIP/resume, limits, **fresh VM checklist**.

Developer quality gates remain in [`../DEVELOPMENT.md`](../DEVELOPMENT.md).
