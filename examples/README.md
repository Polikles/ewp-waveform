# Examples

TOML/JSON here are schema illustrations, not rendered media.

## Later: default-preset demo assets

After renderer testing, the repository should include **short**, separately licensed speech excerpts plus the waveforms those excerpts produce with **built-in default presets** (at least `iuris-default` at 30 fps).

Purpose: show operators and a future GUI what the defaults actually look like, without requiring a full episode.

Constraints (see `CONTRIBUTING.md`):

- no private or complete episodes;
- identify CC BY-NC-SA 4.0 Etyka w Pętli excerpts where used;
- keep clips short;
- do not commit generated workdirs or long ProRes masters if a compact PNG/Web-friendly still+short loop is enough for the GUI.

Until then, local spike renders stay outside the repo.
