schema_version = 1
name = "custom-performance-example"

[processing]
chunk_seconds = 120
jobs = 2  # process-pool scroll chunk encodes; does not change look
ffmpeg_threads = 8

[workdirs]
persistent = true
keep_on_success = true
keep_on_failure = true
root = "/path/to/persistent-work"
